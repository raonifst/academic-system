/* São declarados aqui constantes (enums) que podem ser utilizadas para determinar o status de
 uma determinada função, por exemplo, ao retornar (return). */

export const uploadDataStatus = Object.freeze({
  SUCCESS: 0,
  WARNINGS: 1,
  ERROR: 2
});
